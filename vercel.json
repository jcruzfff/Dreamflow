{
  "version": 2,
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }],
  "builds": [
    { "src": "**", "use": "@vercel/static" },
    { "src": "/api/server.mjs", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/$1" }
  ]
}

